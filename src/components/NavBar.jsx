import styled from "styled-components";

const Navigation = styled.nav`
  padding: 1rem;
  background: #366bac;
  color: white;
  font-weight: bolder;
`;

export default function NavBar() {
  return <Navigation>VGAnalyzed</Navigation>;
}
